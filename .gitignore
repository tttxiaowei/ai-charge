# 依赖
node_modules/

# 构建产物 / 打包输出
dist/
dist-electron/
release/

# 日志
*.log
npm-debug.log*

# 系统 / 编辑器
.DS_Store
Thumbs.db

# Claude Code 本地个人配置（权限白名单等，不随项目共享）
.claude/settings.local.json

# git commit 门禁的通过标记（tester / quality-engineer 落盘，本地运行时产物，不入仓库）
.claude/gates/

# 测试覆盖率报告（vitest --coverage 生成）
coverage/

